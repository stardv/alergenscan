import Foundation

/// Non-English allergen terms for the largest EU label languages.
///
/// This is not decoration. Open Food Facts returns `ingredients_text` in the
/// product's own language — a French jar of Nutella comes back as
/// "LAIT écrémé en poudre, LACTOSERUM en poudre… lécithines (SOJA)" — and OCR
/// of a European package reads whatever is printed on it. Without these terms
/// the label path silently misses milk on any product not sold in English,
/// which is a false negative: the exact failure this app exists to prevent.
///
/// Terms are written **without diacritics**, because `TextNormalizer` folds
/// accents before matching. Write "cacahuete", not "cacahuète".
extension AllergenDictionary {

    static let europeanTerms: [Term] = [

        // ── French ───────────────────────────────────────────────────────
        Term("lait", .milk),
        Term("lait ecreme", .milk),
        Term("lait entier", .milk),
        Term("poudre de lait", .milk),
        Term("petit lait", .milk),
        Term("lactoserum", .milk),
        Term("beurre", .milk),
        Term("creme", .milk),
        Term("fromage", .milk),
        Term("caseine", .milk),
        Term("caseinate", .milk),
        Term("lactique", .milk),
        Term("oeuf", .eggs),
        Term("oeufs", .eggs),
        Term("blanc d oeuf", .eggs),
        Term("jaune d oeuf", .eggs),
        Term("ble", .gluten),
        Term("farine de ble", .gluten),
        Term("orge", .gluten),
        Term("seigle", .gluten),
        Term("avoine", .gluten),
        Term("epeautre", .gluten),
        Term("arachide", .peanuts),
        Term("arachides", .peanuts),
        Term("cacahuete", .peanuts),
        Term("cacahuetes", .peanuts),
        Term("noisette", .treeNuts),
        Term("noisettes", .treeNuts),
        Term("amande", .treeNuts),
        Term("amandes", .treeNuts),
        Term("noix", .treeNuts),
        Term("noix de cajou", .treeNuts),
        Term("pistache", .treeNuts),
        Term("noix de pecan", .treeNuts),
        Term("noix du bresil", .treeNuts),
        Term("cajou", .treeNuts),
        Term("pignons", .treeNuts),
        Term("pate d amande", .treeNuts),
        Term("oeuf entier", .eggs),
        Term("poudre d oeuf", .eggs),
        Term("poisson", .fish),
        Term("anchois", .fish),
        Term("crustaces", .crustaceans),
        Term("crevette", .crustaceans),
        Term("crevettes", .crustaceans),
        Term("mollusques", .molluscs),
        Term("moule", .molluscs),
        Term("huitre", .molluscs),
        Term("celeri", .celery),
        Term("moutarde", .mustard),
        Term("graines de sesame", .sesame),
        Term("sulfites", .sulphites),
        Term("anhydride sulfureux", .sulphites),

        // ── German ───────────────────────────────────────────────────────
        Term("milch", .milk),
        Term("vollmilch", .milk),
        Term("magermilch", .milk),
        Term("milchpulver", .milk),
        Term("milcheiweiss", .milk),
        Term("molke", .milk),
        Term("molkenpulver", .milk),
        Term("laktose", .milk),
        Term("kasein", .milk),
        Term("sahne", .milk),
        Term("rahm", .milk),
        Term("kase", .milk),
        Term("joghurt", .milk),
        Term("ei", .eggs),
        Term("eier", .eggs),
        Term("eigelb", .eggs),
        Term("eiweiss", .eggs),
        Term("volleipulver", .eggs),
        Term("weizen", .gluten),
        Term("weizenmehl", .gluten),
        Term("dinkel", .gluten),
        Term("gerste", .gluten),
        Term("roggen", .gluten),
        Term("hafer", .gluten),
        Term("erdnuss", .peanuts),
        Term("erdnusse", .peanuts),
        Term("haselnuss", .treeNuts),
        Term("haselnusse", .treeNuts),
        Term("mandel", .treeNuts),
        Term("mandeln", .treeNuts),
        Term("walnuss", .treeNuts),
        Term("pistazie", .treeNuts),
        Term("cashewnuss", .treeNuts),
        Term("paranuss", .treeNuts),
        Term("nusse", .treeNuts),
        Term("eipulver", .eggs),
        Term("hunerei", .eggs),
        Term("huhnerei", .eggs),
        Term("vollei", .eggs),
        Term("sojabohnen", .soybeans),
        Term("sojalecithin", .soybeans),
        Term("fisch", .fish),
        Term("sardelle", .fish),
        Term("krebstiere", .crustaceans),
        Term("garnele", .crustaceans),
        Term("weichtiere", .molluscs),
        Term("muschel", .molluscs),
        Term("auster", .molluscs),
        Term("sellerie", .celery),
        Term("senf", .mustard),
        Term("sesam", .sesame),
        Term("sulfite", .sulphites),
        Term("schwefeldioxid", .sulphites),
        Term("lupine", .lupin),

        // ── Spanish ──────────────────────────────────────────────────────
        Term("leche", .milk),
        Term("leche desnatada", .milk),
        Term("leche en polvo", .milk),
        Term("mantequilla", .milk),
        Term("nata", .milk),
        Term("queso", .milk),
        Term("yogur", .milk),
        Term("suero de leche", .milk),
        Term("lactosa", .milk),
        Term("caseina", .milk),
        Term("huevo", .eggs),
        Term("huevos", .eggs),
        Term("clara de huevo", .eggs),
        Term("trigo", .gluten),
        Term("harina de trigo", .gluten),
        Term("cebada", .gluten),
        Term("centeno", .gluten),
        Term("espelta", .gluten),
        Term("mani", .peanuts),
        Term("avellana", .treeNuts),
        Term("avellanas", .treeNuts),
        Term("almendra", .treeNuts),
        Term("almendras", .treeNuts),
        Term("anacardo", .treeNuts),
        Term("pistacho", .treeNuts),
        Term("nueces", .treeNuts),
        Term("nuez", .treeNuts),
        Term("pinones", .treeNuts),
        Term("frutos secos", .treeNuts),
        Term("huevo en polvo", .eggs),
        Term("huevo entero", .eggs),
        Term("pescado", .fish),
        Term("anchoa", .fish),
        Term("crustaceos", .crustaceans),
        Term("gamba", .crustaceans),
        Term("gambas", .crustaceans),
        Term("camaron", .crustaceans),
        Term("moluscos", .molluscs),
        Term("mejillon", .molluscs),
        Term("ostra", .molluscs),
        Term("apio", .celery),
        Term("mostaza", .mustard),
        Term("ajonjoli", .sesame),
        Term("sulfitos", .sulphites),
        Term("dioxido de azufre", .sulphites),
        Term("altramuz", .lupin),

        // ── Italian ──────────────────────────────────────────────────────
        Term("latte", .milk),
        Term("latte scremato", .milk),
        Term("latte in polvere", .milk),
        Term("burro", .milk),
        Term("panna", .milk),
        Term("formaggio", .milk),
        Term("siero di latte", .milk),
        Term("lattosio", .milk),
        Term("caseinato", .milk),
        Term("uovo", .eggs),
        Term("uova", .eggs),
        Term("albume", .eggs),
        Term("frumento", .gluten),
        Term("farina di frumento", .gluten),
        Term("orzo", .gluten),
        Term("segale", .gluten),
        Term("farro", .gluten),
        Term("arachidi", .peanuts),
        Term("nocciola", .treeNuts),
        Term("nocciole", .treeNuts),
        Term("mandorla", .treeNuts),
        Term("mandorle", .treeNuts),
        Term("anacardi", .treeNuts),
        Term("pistacchio", .treeNuts),
        Term("noci", .treeNuts),
        Term("pinoli", .treeNuts),
        Term("frutta a guscio", .treeNuts),
        Term("uovo intero", .eggs),
        Term("albume d uovo", .eggs),
        Term("soia", .soybeans),
        Term("pesce", .fish),
        Term("acciuga", .fish),
        Term("acciughe", .fish),
        Term("crostacei", .crustaceans),
        Term("gambero", .crustaceans),
        Term("granchio", .crustaceans),
        Term("molluschi", .molluscs),
        Term("cozza", .molluscs),
        Term("ostrica", .molluscs),
        Term("sedano", .celery),
        Term("senape", .mustard),
        Term("sesamo", .sesame),
        Term("solfiti", .sulphites),
        Term("anidride solforosa", .sulphites),
        Term("lupino", .lupin),
    ]
}
